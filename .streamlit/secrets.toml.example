# Copy to secrets.toml and fill in. On Streamlit Cloud, set these in the app dashboard.
# Get a token: GitHub → Settings → Developer settings → Personal access tokens (repo scope).

GITHUB_TOKEN = "ghp_xxxxxxxxxxxx"
GITHUB_REPO = "fabix99/Home-Training-No-Blockers"

# Edit workouts is enabled only for Begum and Fabio URLs (?token=); no secret needed.
